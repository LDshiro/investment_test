# 20-trade-day historical shadow replay profile.
# Extends the generic local batch profile and replays the latest 20 valid trade dates
# inside the Table 1 exact-match sample window.

extends:
  - shadow_corrected_batch_local.yaml

run:
  name: shadow_corrected_batch_20d_local

batch:
  end_date: 2025-11-28
  max_days: 20
