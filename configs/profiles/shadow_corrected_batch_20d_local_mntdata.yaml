# Absolute-path version of the 20-trade-day batch profile for the current container session.

extends:
  - shadow_corrected_batch_local_mntdata.yaml

run:
  name: shadow_corrected_batch_20d_local

batch:
  end_date: 2025-11-28
  max_days: 20
